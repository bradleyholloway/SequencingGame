{
  "name": "sequencing",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "pnpm -r --filter ./apps/** --filter ./services/** build",
    "dev": "echo Use filtered dev scripts, e.g., pnpm --filter apps/web dev && pnpm --filter services/realtime dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
